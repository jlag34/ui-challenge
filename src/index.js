import React from 'react';
import ReactDOM from 'react-dom';

import App from './components/app';

//Very simple index file since we arent using Redux

ReactDOM.render(<App />, document.querySelector('#root')
);
